import random
list_xp = []
list_yp = []

page = 0

def setup():
    size(500,500)
    



def draw():
    if page == 0:
        background("#FFEFD5")
        stroke("#FFFF00")
        fill("#00000")
        textSize(22)
        text("Welcome to minesweeper", CENTER, 40)
        
        textSize(12)
        text("Double click play to play the game",CENTER,80)
        text("Try not click on the mine to win the game",CENTER,100)
        text("Good luck! Have fun!",CENTER,120)
        
        fill ("#FFFF33")  
        rect(10,200,100,25)
        fill ("#00000")   
        textSize(18)
        text("       Play",CENTER,218) 
    elif page == 2:
        for i in range(1):
            xp = str (random.randrange(25, 500, 50))
            yp = str (random.randrange(25, 500, 50))
            print(xp,yp)
                        
            int_xp = float(xp)
            int_yp = float(yp)
            lr = 50
            wr = 50
            for x in range(0,500,50):
                for y in range(0,500,50):
                    fill(255,255,255)
                    rect(x,y,lr,wr)    
            fill(0,0,0)
            ellipse(xp,yp,30,30)

def mousePressed():
    global page
    page += 1
